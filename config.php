<?php
/**
 * config.php (الجذر) — ملف تحويل فقط
 * ══════════════════════════════════════════════════════════════
 *
 * لماذا أصبح هذا الملف مجرد تحويلة؟
 *
 * كان المشروع يحتوي نسختين كاملتين ومتطابقتين تقريباً من ملف
 * الإعدادات: هذا الملف و core/config.php. كلاهما يعرّف نفس الدوال
 * (db, sanitizeInput, csrfToken, rateLimit, cacheGet ...) بلا أي
 * حماية function_exists.
 *
 * الخطران اللذان كانا قائمين:
 *   ① لو حمّل أي ملف النسختين معاً في نفس الطلب لحصل خطأ قاتل
 *      "Cannot redeclare function" وتوقّف الموقع بالكامل.
 *   ② عند إصلاح ثغرة أو تعديل إعداد في نسخة واحدة فقط، تبقى النسخة
 *      الأخرى قديمة — وهذا مصدر أعطال يصعب تتبّعها لاحقاً.
 *
 * الحل: مصدر واحد للحقيقة هو core/config.php، وهذا الملف يحوّل إليه
 * حتى لا ينكسر أي كود قديم يستدعي config.php من الجذر.
 *
 * النسخة الأصلية محفوظة في: _backup_before_fix/config.root.php
 */

require_once __DIR__ . '/core/config.php';
